<!DOCTYPE html>
<!--[if IE 8]> <html lang="en" class="ie8"> <![endif]-->
<!--[if IE 9]> <html lang="en" class="ie9"> <![endif]-->
<!--[if !IE]><!--> <html lang="ja"> <!--<![endif]-->  
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <title><%= @page_title ? @page_title : 'Welcome' %> | The Bookstore</title>

  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <nav class="navbar navbar-default navbar-fixed-top">
    <div class="container">
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
      </div>
      <div id="navbar" class="collapse navbar-collapse">
        <ul class="nav navbar-nav">
          <!-- どのページでも、rootへのリンクにクラス activeがついてしまっているのを、
          アクセスしているページによってactiveがつくかどうかを変える。  -->
          <!-- <li class="active"><%=  link_to 'Home', root_path %></li> -->
          <!-- アクセスしているurlを処理するcontrollerが booksControllerなら、
          classをactiveに、そうでないなら、classを''、つまり無しにする。 -->
          <li class="<%= params[:controller] == 'books' ? 'active' : '' %>"><%= link_to 'Home', root_path %></li>
          <li class="<%= params[:controller] == 'about' ? 'active' : '' %>"><%= link_to 'About Us', about_path %></li>
        </ul>
        <ul class="nav navbar-nav navbar-right">
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Add New<span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><%= link_to 'Book', new_book_path %></li>
              <li><%= link_to 'Category', new_category_path %></li>
              <li><%= link_to 'Author', new_author_path %></li>
              <li><%= link_to 'Publisher', new_publisher_path %></li>
            </ul>
          </li>
        </ul>
      </div><!--/.nav-collapse -->
    </div>
  </nav>

  <header>
    <div class="container">
      <!-- @page_titileの中身があればそれを表示、
      無ければ Welcome を表示させる。 ternary -->
      <h1>The BookStore <small><%= @page_title ? @page_title : 'Welcome' %></small></h1>
    </div>
  </header>

  <section>
    <div class="container">
      <div class="row">
        <div class="col-md-8">
          <%= yield %>
        </div>
        <div class="col-md-4">
          <%= render "partials/sidebar" %>
        </div>
      </div>
    </div>
  </section>

  <footer>
    <div class="container">
      <p>Copyright &copy; 2015, All Rights Reserved</p>
    </div>
  </footer>

</body>
</html>
